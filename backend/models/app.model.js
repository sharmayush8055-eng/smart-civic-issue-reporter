const mongoose=require ("mongoose");
const postSchema=new Schema=new mangoose.Schem({
    image:String,
    Description:String

})
const postModel=MongooseError.model("post",postSchema);
module.exports=postModel;